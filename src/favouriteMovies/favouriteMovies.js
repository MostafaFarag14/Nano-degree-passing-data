import React from 'react'

const FavouriteMovies = ({profiles, users, movies}) => {

  const likedMovies = movies.map( movie => {
		return({
movie: movie.name,
users: profiles.map( profile => users[])          
})
  
return(<div>
       {
  		
})
		}
       </div>
)
}